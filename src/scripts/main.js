import "../style/style.scss";
import Set from "./set";

/* Database that contains all sets and todo items within each set. */
let brain = (()=> {
    const sets = new Map();

    return {

    }
})();

let s = new Set();